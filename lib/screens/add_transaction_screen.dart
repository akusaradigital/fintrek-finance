import 'package:flutter/material.dart';
class AddTransactionScreen extends StatelessWidget { const AddTransactionScreen({super.key}); @override Widget build(BuildContext context) => DefaultTabController(length: 3, child: Scaffold(appBar: AppBar(title: const Text('Quick Add'), bottom: const TabBar(tabs: [Tab(text: 'Expense'), Tab(text: 'Income'), Tab(text: 'Transfer')])), body: const TabBarView(children: [_Form(type: 'Expense'), _Form(type: 'Income'), _Form(type: 'Transfer')]))); }
class _Form extends StatelessWidget { const _Form({required this.type}); final String type; @override Widget build(BuildContext context) => ListView(padding: const EdgeInsets.all(16), children: [Text(type, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.w700)), const SizedBox(height: 12), const TextField(decoration: InputDecoration(labelText: 'Amount')), const SizedBox(height: 12), const TextField(decoration: InputDecoration(labelText: 'Category')), const SizedBox(height: 12), const TextField(decoration: InputDecoration(labelText: 'Wallet')), const SizedBox(height: 12), const TextField(decoration: InputDecoration(labelText: 'Note')), const SizedBox(height: 12), FilledButton(onPressed: () {}, child: const Text('Save')) ]); }
